# Exercise 5 Output

## Expected Result
- The bean is fetched twice from the same application context.
- The output confirms both references point to the same singleton instance.

## Sample Output
```text
Country constructor called
Same instance? true
```
