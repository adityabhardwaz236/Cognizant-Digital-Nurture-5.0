# Hands-on 1: Create a Spring Web Project using Maven

This project follows the Spring Initializr setup:
- Group: com.cognizant
- Artifact: spring-learn
- Dependencies: Spring Web, Spring Boot DevTools

Run the application using:
```bash
mvn spring-boot:run
```
